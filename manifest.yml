---
applications:
- name: material-tic-tac-toe
  random-route: false
  buildpacks:
    - nodejs_buildpack
  memory: 256M
  stack: cflinuxfs4
  command: npm install && npm i -g @angular/cli && ng serve
